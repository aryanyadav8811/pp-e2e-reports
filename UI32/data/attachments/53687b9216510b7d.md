# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: slip_sharing/slip_sharing_and_tailing.spec.ts >> Slip sharing & tailing >> Logged-out tailer signs in via the tail link, then auto-tails and submits
- Location: tests/slip_sharing/slip_sharing_and_tailing.spec.ts:173:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: 'Share Slip' }).filter({ visible: true }).first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('button', { name: 'Share Slip' }).filter({ visible: true }).first()

```

# Test source

```ts
  1  | import { BasePage } from "./base.page";
  2  | import { expect } from "@playwright/test";
  3  | 
  4  | export class ContestSuccessPage extends BasePage {
  5  |   // locators starts
  6  | 
  7  |   correctLabel = this.byText("Correct", { exact: true });
  8  |   correctPicksNumbers = this.correctLabel.locator("..").locator("div").nth(1);
  9  |   continueBtn = this.byRole("button", { name: "Continue" });
  10 |   shareSlipBtn = this.byRole("button", { name: "Share Slip" });
  11 | 
  12 |   //locators ends
  13 | 
  14 |   async getPickNumbers() {
  15 |     const picks = await this.correctPicksNumbers.textContent();
  16 |     const match = picks?.match(/(\d+)\s*\/\s*(\d+)/);
  17 | 
  18 |     if (!match) throw new Error("Could not parse correct score");
  19 |     return { toBecorrect: +match[1], total: +match[2] };
  20 |   }
  21 | 
  22 |   async verifyPicksNumbers(toBeCorrectPicks: number, totalPicks: number) {
  23 |     const { toBecorrect, total } = await this.getPickNumbers();
  24 |     await expect(toBecorrect).toBe(toBeCorrectPicks);
  25 |     await expect(total).toBe(totalPicks);
  26 |   }
  27 | 
  28 |   async continueToHomePage() {
  29 |     await this.continueBtn.click();
  30 |   }
  31 | 
  32 |   async assertContestShareable() {
> 33 |     await expect(this.shareSlipBtn).toBeVisible();
     |                                     ^ Error: expect(locator).toBeVisible() failed
  34 |   }
  35 | }
  36 | 
```