# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: slip_sharing/slip_sharing_and_tailing.spec.ts >> Slip sharing & tailing >> Logged-out tailer signs in via the tail link, then auto-tails and submits
- Location: tests/slip_sharing/slip_sharing_and_tailing.spec.ts:173:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: /log in to tail/i }).filter({ visible: true }).first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('button', { name: /log in to tail/i }).filter({ visible: true }).first()

```

# Test source

```ts
  1  | import { type Page, expect } from "@playwright/test";
  2  | import { BasePage } from "./base.page";
  3  | 
  4  | export class TailPage extends BasePage {
  5  |   readonly tailThisEntryBtn = this.byRole("button", {
  6  |     name: /tail this entry/i,
  7  |   });
  8  |   readonly loginToTailBtn = this.byRole("button", { name: /log in to tail/i });
  9  |   readonly overrideConfirmBtn = this.byRole("button", {
  10 |     name: /override & tail/i,
  11 |   });
  12 | 
  13 |   constructor(page: Page) {
  14 |     super(page);
  15 |   }
  16 | 
  17 |   async openByUuid(uuid: string): Promise<void> {
  18 |     await this.open(`/tail/${uuid}`);
  19 |   }
  20 | 
  21 |   async openByPath(path: string): Promise<void> {
  22 |     await this.open(path);
  23 |   }
  24 | 
  25 |   async assertReadyToTail(): Promise<void> {
  26 |     await expect(this.tailThisEntryBtn).toBeVisible();
  27 |   }
  28 | 
  29 |   async assertReadyToLogIn(): Promise<void> {
> 30 |     await expect(this.loginToTailBtn).toBeVisible();
     |                                       ^ Error: expect(locator).toBeVisible() failed
  31 |   }
  32 | 
  33 |   async clickTailThisEntry(): Promise<void> {
  34 |     await this.tailThisEntryBtn.click();
  35 |   }
  36 | 
  37 |   async clickLoginToTail(): Promise<void> {
  38 |     await this.loginToTailBtn.click();
  39 |   }
  40 | 
  41 |   // click the over ride existing slip modal
  42 |   async confirmOverrideIfPresent(timeoutMs = 5_000): Promise<boolean> {
  43 |     const present = await this.overrideConfirmBtn
  44 |       .isVisible({ timeout: timeoutMs })
  45 |       .catch(() => false);
  46 |     if (!present) return false;
  47 |     await this.overrideConfirmBtn.click();
  48 |     return true;
  49 |   }
  50 | }
  51 | 
```