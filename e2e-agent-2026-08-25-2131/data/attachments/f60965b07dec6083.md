# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth/login.spec.ts >> Login - Page Rendering >> password field masks input
- Location: tests/auth/login.spec.ts:50:3

# Error details

```
Error: expect(locator).toHaveAttribute(expected) failed

Locator: locator('#password').filter({ visible: true }).first()
Expected: "password"
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toHaveAttribute" with timeout 15000ms
  - waiting for locator('#password').filter({ visible: true }).first()

```

```yaml
- main:
  - heading "staging.parlayplay.io" [level=1]
  - heading "Performing security verification" [level=2]
  - paragraph: This website uses a security service to protect against malicious bots. This page is displayed while the website verifies you are not a bot.
- contentinfo:
  - text: "Ray ID:"
  - code: a30bb9038d2e3e04
  - text: Performance and Security by
  - link "Cloudflare, opens in a new tab":
    - /url: https://www.cloudflare.com?utm_source=challenge&utm_campaign=m
    - text: Cloudflare
  - link "Privacy, opens in a new tab":
    - /url: https://www.cloudflare.com/privacypolicy/
    - text: Privacy
```

# Test source

```ts
  1   | import { test, expect } from "../../fixtures/test.extend";
  2   | import { AuthPage } from "../../pages/auth.page";
  3   | import { loginMatrix } from "../../utils/loginData";
  4   | import { HomePage } from "@pages/home.page";
  5   | import { MenuPage } from "@pages/menu.page";
  6   | import { LogOutPage } from "@pages/logout.page";
  7   | 
  8   | const validUsernameCase = loginMatrix.find(
  9   |   (tc) => tc.shouldSucceed && tc.isPrimary
  10  | )!;
  11  | const validEmailCase = loginMatrix.find(
  12  |   (tc) => tc.shouldSucceed && !tc.isPrimary && tc.title.includes("email")
  13  | )!;
  14  | const invalidPasswordCase = loginMatrix.find(
  15  |   (tc) => !tc.shouldSucceed && tc.title === "invalid password"
  16  | )!;
  17  | const unknownUserCase = loginMatrix.find(
  18  |   (tc) => !tc.shouldSucceed && tc.title === "unknown user"
  19  | )!;
  20  | const emptyUsernameCase = loginMatrix.find(
  21  |   (tc) => !tc.shouldSucceed && tc.title === "empty username"
  22  | )!;
  23  | const emptyPasswordCase = loginMatrix.find(
  24  |   (tc) => !tc.shouldSucceed && tc.title === "empty password"
  25  | )!;
  26  | 
  27  | test.describe('Login - Page Rendering', {tag: "@auth"}, () => {
  28  |   // Pure navigation/rendering checks — no logins, safe to parallelize.
  29  |   test.describe.configure({ mode: 'parallel' });
  30  | 
  31  |   test('login page renders all expected elements', async ({ page }) => {
  32  |     const auth = new AuthPage(page);
  33  | 
  34  |     await auth.openLogIn();
  35  | 
  36  |     await auth.assertOnLoginPage();
  37  |     await expect(auth.forgotPasswordLink).toBeVisible();
  38  |   });
  39  | 
  40  |   test("login page is accessible via direct URL navigation", async ({
  41  |     page,
  42  |   }) => {
  43  |     const auth = new AuthPage(page);
  44  | 
  45  |     await page.goto("/account/login");
  46  | 
  47  |     await auth.assertOnLoginPage();
  48  |   });
  49  | 
  50  |   test("password field masks input", async ({ page }) => {
  51  |     const auth = new AuthPage(page);
  52  | 
  53  |     await auth.openLogIn();
  54  | 
> 55  |     await expect(auth.passwordField).toHaveAttribute("type", "password");
      |                                      ^ Error: expect(locator).toHaveAttribute(expected) failed
  56  |   });
  57  | });
  58  | 
  59  | test.describe("Login - Valid Credentials", () => {
  60  |   // Real logins hit Cloudflare's rate limit when they overlap. Run them one at
  61  |   // a time; auth.submit() backs off and retries on any 429 that still slips
  62  |   // through.
  63  |   test.describe.configure({ mode: 'serial' });
  64  | 
  65  |   test(
  66  |     "user can login with valid username",
  67  |     { tag: "@smoke" },
  68  |     async ({ page }) => {
  69  |       const auth = new AuthPage(page);
  70  | 
  71  |       await test.step("Login with valid username credentials", async () => {
  72  |         await auth.logIn(
  73  |           validUsernameCase.username,
  74  |           validUsernameCase.password
  75  |         );
  76  |       });
  77  | 
  78  |       await test.step("Assert login success", async () => {
  79  |         await auth.assertSuccess();
  80  |       });
  81  |     }
  82  |   );
  83  | 
  84  |   test(
  85  |     "user can login with valid email",
  86  |     { tag: "@smoke" },
  87  |     async ({ page }) => {
  88  |       const auth = new AuthPage(page);
  89  | 
  90  |       await test.step("Login with valid email credentials", async () => {
  91  |         await auth.logIn(validEmailCase.username, validEmailCase.password);
  92  |       });
  93  | 
  94  |       await test.step("Assert login success", async () => {
  95  |         await auth.assertSuccess();
  96  |       });
  97  |     }
  98  |   );
  99  | 
  100 |   test("user is redirected to home after successful login", async ({
  101 |     page,
  102 |   }) => {
  103 |     const auth = new AuthPage(page);
  104 |     const homePage = new HomePage(page);
  105 | 
  106 |     await auth.logIn(validUsernameCase.username, validUsernameCase.password);
  107 |     await auth.assertSuccess();
  108 | 
  109 |     await homePage.assertHomePage();
  110 |   });
  111 | 
  112 |   test('login respects "next" query param redirect', async ({ page }) => {
  113 |     const auth = new AuthPage(page);
  114 | 
  115 |     await page.goto("/account/login?next=/account/update");
  116 | 
  117 |     await auth.usernameField.fill(validUsernameCase.username);
  118 |     await auth.passwordField.fill(validUsernameCase.password);
  119 |     await auth.submit();
  120 | 
  121 |     await expect(page).toHaveURL(/\/account\/update/);
  122 |   });
  123 | });
  124 | 
  125 | test.describe("Login - Invalid Credentials", { tag: "@auth" }, () => {
  126 |   // Serialize the login-heavy block and keep a small pre-login pause; the real
  127 |   // safety net is auth.submit(), which honors Cloudflare's backoff on a 429.
  128 |   test.describe.configure({ mode: 'serial' });
  129 |   const RATE_LIMIT_COOLDOWN_MS = 30_000;
  130 |   test.beforeEach(async ({ page }) => {
  131 |     await page.waitForTimeout(RATE_LIMIT_COOLDOWN_MS);
  132 |   });
  133 | 
  134 |   test("cannot login with invalid password", async ({ page }) => {
  135 |     const auth = new AuthPage(page);
  136 | 
  137 |     await auth.logIn(
  138 |       invalidPasswordCase.username,
  139 |       invalidPasswordCase.password
  140 |     );
  141 | 
  142 |     await auth.assertFailure(invalidPasswordCase.expectedMessage);
  143 |   });
  144 | 
  145 |   test("cannot login with unknown username", async ({ page }) => {
  146 |     const auth = new AuthPage(page);
  147 | 
  148 |     await auth.logIn(unknownUserCase.username, unknownUserCase.password);
  149 | 
  150 |     await auth.assertFailure(unknownUserCase.expectedMessage);
  151 |   });
  152 | 
  153 |   test("cannot login with empty username", async ({ page }) => {
  154 |     const auth = new AuthPage(page);
  155 | 
```