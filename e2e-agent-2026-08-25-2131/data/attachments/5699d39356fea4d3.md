# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: navigation/footer-age-badge.spec.ts >> Footer age badge — 18+ (UI Release 32) >> Home footer shows the 18+ age badge and no 21+ anywhere
- Location: tests/navigation/footer-age-badge.spec.ts:18:5

# Error details

```
TimeoutError: locator.waitFor: Timeout 30000ms exceeded.
Call log:
  - waiting for locator('[data-testid="cross-game-card-wrapper"]').filter({ visible: true }).first() to be visible

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - main [ref=e2]:
    - generic [ref=e3]:
      - heading "staging.parlayplay.io" [level=1] [ref=e5]
      - heading "Performing security verification" [level=2] [ref=e6]
      - paragraph [ref=e7]: This website uses a security service to protect against malicious bots. This page is displayed while the website verifies you are not a bot.
  - contentinfo [ref=e12]:
    - generic [ref=e14]:
      - generic [ref=e16]:
        - text: "Ray ID:"
        - code [ref=e17]: a30b3f391da9da6e
      - generic [ref=e18]:
        - generic [ref=e19]:
          - text: Performance and Security by
          - link "Cloudflare, opens in a new tab" [ref=e20] [cursor=pointer]:
            - /url: https://www.cloudflare.com?utm_source=challenge&utm_campaign=m
            - text: Cloudflare
        - link "Privacy, opens in a new tab" [ref=e22] [cursor=pointer]:
          - /url: https://www.cloudflare.com/privacypolicy/
          - text: Privacy
```

# Test source

```ts
  97  |   // only mobile's container has the league-selector testid on builds
  98  |   // predating the desktop wrapper's testid — so anchor on the button ids
  99  |   // (visible-filtered to skip the hidden off-breakpoint variant) instead of
  100 |   // the container.
  101 |   readonly leagueButtons = this.page
  102 |     .locator('button[id^="league-"]')
  103 |     .filter({ visible: true });
  104 |   readonly warningModal = this.visible(
  105 |     this.page.locator('div[role="dialog"]').filter({ hasText: "Pick Warning" })
  106 |   );
  107 |   readonly toggleMenu = this.byRole("button", { name: "Toggle Menu" });
  108 |   // DFS-2467: button on mobile, link (with an optional badge-count suffix in
  109 |   // the accessible name) on desktop.
  110 |   readonly rewardsTab = this.visible(
  111 |     this.page
  112 |       .getByRole("button", { name: "Rewards" })
  113 |       .or(this.page.getByRole("link", { name: /^Rewards\b/ }))
  114 |   );
  115 |   readonly fgleagueTabs = this.page.locator(
  116 |     '[data-testid="league-selector"] button[id^="league-"]:not([id*="combo"]):not([id*="H1"]):not([id*="Q1"])'
  117 |   );
  118 |   readonly statsContainer = this.page.locator(
  119 |     "div.flex.flex-col.items-center >> ul.flex.flex-row"
  120 |   );
  121 | 
  122 |   readonly statsTabs = this.statsContainer.locator(":scope > li > button");
  123 | 
  124 |   constructor(page: Page) {
  125 |     super(page);
  126 |   }
  127 | 
  128 |   nthPlayerOnDefault(index: number): Locator {
  129 |     return this.playerCardsVisible.nth(index);
  130 |   }
  131 | 
  132 |   /**
  133 |    * Card locator anchored on its stable `id="player-<id>"`. The virtualised
  134 |    * grid unmounts/remounts cards as it scrolls, so index-based handles
  135 |    * (`nth(i)`) silently re-resolve to a different card mid-flow — an
  136 |    * id-anchored locator always points at the same player (or at nothing once
  137 |    * it scrolls out of the mount window).
  138 |    */
  139 |   playerCardById(playerId: string): Locator {
  140 |     return this.visible(this.page.locator(`div[id="${playerId}"]`));
  141 |   }
  142 | 
  143 |   /** Ids of the currently mounted player cards, in DOM order. */
  144 |   async listVisiblePlayerIds(): Promise<string[]> {
  145 |     await this.waitForPlayersGrid();
  146 |     await expect(this.playerCardsVisible.first()).toBeVisible();
  147 |     return (
  148 |       await this.playerCardsVisible.evaluateAll((els) => els.map((el) => el.id))
  149 |     ).filter(Boolean);
  150 |   }
  151 | 
  152 |   playerStatButton(id: string): Locator {
  153 |     return this.visible(
  154 |       this.page.locator(id).locator('button:has([data-testid="stat-value"])')
  155 |     );
  156 |   }
  157 | 
  158 |   /**
  159 |    * DFS-2467: the more/less pick buttons carry `grid-button` in the mobile
  160 |    * card (components/CrossGameCardAlt/Mobile/row.tsx) but
  161 |    * `stat-selection-button` in the desktop card (CrossGameCardAlt/row.tsx).
  162 |    * A card locator only ever resolves inside one tree, so the union is safe
  163 |    * on both breakpoints.
  164 |    */
  165 |   pickButtons(card: Locator): Locator {
  166 |     return card
  167 |       .getByTestId("grid-button")
  168 |       .or(card.getByTestId("stat-selection-button"));
  169 |   }
  170 | 
  171 |   moreLessDefaultPlayerButtons(index: number): Locator {
  172 |     return this.pickButtons(this.nthPlayerOnDefault(index));
  173 |   }
  174 | 
  175 |   async openTermsPageUsingNonAuth(
  176 |     button: "Sign Up" | "Join Now"
  177 |   ): Promise<void> {
  178 |     if (button == "Sign Up") {
  179 |       await this.signupButton.click();
  180 |     } else if (button == "Join Now") {
  181 |       await this.joinNowButton.click();
  182 |     }
  183 |   }
  184 | 
  185 |   async waitForPlayersGrid(): Promise<void> {
  186 |     // DFS-2467: the mobile tree nests the grid as the wrapper's 2nd child,
  187 |     // but the desktop tree's wrapper has a different inner shape — anchor on
  188 |     // the outcome (a mounted card, or the empty-state label for leagues with
  189 |     // no offerings) instead of the wrapper's structure.
  190 |     await expect(
  191 |       this.playerCardsVisible.first().or(this.noPlayerFoundVisible).first()
  192 |     ).toBeVisible();
  193 |   }
  194 | 
  195 |   async waitForFeedReady(timeout = 30_000): Promise<void> {
  196 |     await Promise.race([
> 197 |       this.crossGameCardWrapper.waitFor({ state: "visible", timeout }),
      |                                 ^ TimeoutError: locator.waitFor: Timeout 30000ms exceeded.
  198 |       this.noPlayerFoundVisible.waitFor({ state: "visible", timeout }),
  199 |     ]);
  200 |   }
  201 | 
  202 |   async isEmptyState(): Promise<boolean> {
  203 |     return this.noPlayerFoundVisible.isVisible().catch(() => false);
  204 |   }
  205 | 
  206 |   async getPlayerCardCount(): Promise<number> {
  207 |     await this.waitForPlayersGrid();
  208 |     const cards = this.playerCardsVisible;
  209 |     await expect(cards.first()).toBeVisible();
  210 |     return await cards.count();
  211 |   }
  212 | 
  213 |   async listPlayersCard(): Promise<Locator[]> {
  214 |     await this.waitForPlayersGrid();
  215 |     const cards = this.playerCardsVisible;
  216 |     await expect(cards.first()).toBeVisible();
  217 |     const count = await cards.count();
  218 |     const result: Locator[] = [];
  219 |     for (let i = 0; i < count; i++) {
  220 |       result.push(cards.nth(i));
  221 |     }
  222 |     return result;
  223 |   }
  224 | 
  225 |   async getLeagueCount(): Promise<number> {
  226 |     return await this.leagueButtons.count();
  227 |   }
  228 | 
  229 |   /**
  230 |    * WebKit occasionally leaves a player-card image overlapping the league
  231 |    * pill row after a submit → return-to-home cycle, and an unbounded click
  232 |    * then retries against the intercepting <img> until the whole test times
  233 |    * out (observed: 240s hang on Mobile Safari). Bound the normal click,
  234 |    * retry after scrolling back to the top (which restores the row's hit
  235 |    * area), and only then fall back to a forced click.
  236 |    */
  237 |   async clickLeagueTab(btn: Locator): Promise<void> {
  238 |     try {
  239 |       await btn.click({ timeout: 10_000 });
  240 |       return;
  241 |     } catch {
  242 |       /* intercepted — try to restore the layout */
  243 |     }
  244 |     await this.page.evaluate(() => window.scrollTo(0, 0));
  245 |     try {
  246 |       await btn.click({ timeout: 10_000 });
  247 |       return;
  248 |     } catch {
  249 |       /* still intercepted — last resort below */
  250 |     }
  251 |     await btn.click({ force: true, timeout: 10_000 });
  252 |   }
  253 | 
  254 |   async listLeagueButtons(): Promise<Locator[]> {
  255 |     const n = await this.leagueButtons.count();
  256 |     const leagueButtonsLocator: Locator[] = [];
  257 |     for (let i = 0; i < n; i++) {
  258 |       leagueButtonsLocator.push(this.leagueButtons.nth(i));
  259 |     }
  260 |     return leagueButtonsLocator;
  261 |   }
  262 | 
  263 |   async isContinueEnabled(): Promise<boolean> {
  264 |     const button = this.continueBtn;
  265 |     const isPresent = await button.isVisible().catch(() => false);
  266 |     if (!isPresent) return false;
  267 |     const enabled = await button.isEnabled().catch(() => false);
  268 |     return enabled;
  269 |   }
  270 | 
  271 |   async trySelectPick(card: Locator): Promise<boolean> {
  272 |     const buttons = this.pickButtons(card);
  273 |     const lessButton = buttons.first();
  274 |     // Bounded checks: league/stat filter updates run inside useTransition and
  275 |     // the grid is virtualised, so a card captured from listVisiblePlayerIds
  276 |     // can unmount before we act on it. With no actionTimeout configured, an
  277 |     // unbounded isEnabled()/click() on a gone card would hang the test —
  278 |     // treat "card no longer there" as an unpickable card instead.
  279 |     let isEnabled: boolean;
  280 |     try {
  281 |       isEnabled = await lessButton.isEnabled({ timeout: 5000 });
  282 |     } catch {
  283 |       return false;
  284 |     }
  285 | 
  286 |     try {
  287 |       await buttons.nth(isEnabled ? 0 : 1).click({ timeout: 10_000 });
  288 |     } catch {
  289 |       return false;
  290 |     }
  291 | 
  292 |     //warning modal check
  293 |     try {
  294 |       const modal = this.warningModal;
  295 |       if (await modal.isVisible({ timeout: 2000 }).catch(() => false)) {
  296 |         await modal.getByRole("button", { name: "Understood" }).click();
  297 |         return false;
```