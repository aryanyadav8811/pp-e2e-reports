# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: modals/userlimits.spec.ts >> User limit modal >> Verify user limit modal
- Location: tests/modals/userlimits.spec.ts:18:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('#entryAmountDay').filter({ visible: true }).first()
Expected: visible
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('#entryAmountDay').filter({ visible: true }).first()

```

# Test source

```ts
  1   | import { expect, Locator, Page } from "@playwright/test";
  2   | import { BasePage } from "./base.page";
  3   | 
  4   | export class SettingsPage extends BasePage {
  5   |   readonly form: Locator;
  6   |   readonly username: Locator;
  7   |   readonly email: Locator;
  8   |   readonly firstName: Locator;
  9   |   readonly lastName: Locator;
  10  |   readonly memberSince: Locator;
  11  |   readonly dayAmountInput: Locator;
  12  |   readonly feedVisibleCheckbox: Locator;
  13  |   readonly saveBtn: Locator;
  14  |   readonly confirmBtn: Locator;
  15  |   readonly continueBtn: Locator;
  16  | 
  17  |   constructor(page: Page) {
  18  |     super(page);
  19  | 
  20  |     // locators start
  21  |     this.form = this.locator("form.my-2");
  22  |     this.username = this.form
  23  |       .locator('div:has-text("Username")')
  24  |       .last()
  25  |       .locator("xpath=following-sibling::div[1]");
  26  |     this.email = this.form
  27  |       .locator('div:has-text("Email")')
  28  |       .last()
  29  |       .locator("xpath=following-sibling::div[1]");
  30  |     this.firstName = this.form
  31  |       .locator('div:has-text("First Name")')
  32  |       .last()
  33  |       .locator("xpath=following-sibling::div[1]");
  34  |     this.lastName = this.form
  35  |       .locator('div:has-text("Last Name")')
  36  |       .last()
  37  |       .locator("xpath=following-sibling::div[1]");
  38  |     this.memberSince = this.form
  39  |       .locator('div:has-text("Member Since")')
  40  |       .last()
  41  |       .locator("xpath=following-sibling::div[1]");
  42  |     this.dayAmountInput = this.locator("#entryAmountDay");
  43  |     // `Show My Entries In The Feed` toggle — name=is_feed_visible on the backend.
  44  |     this.feedVisibleCheckbox = this.page.locator("input#feedVisible");
  45  |     this.saveBtn = this.byRole("button", { name: "Save" });
  46  |     this.confirmBtn = this.byRole("button", { name: "Confirm" });
  47  |     this.continueBtn = this.byRole("button", { name: "Continue" });
  48  |     // locators end
  49  |   }
  50  | 
  51  |   async getUserStats() {
  52  |     return {
  53  |       username: await this.username.textContent(),
  54  |       email: await this.email.textContent(),
  55  |       firstName: await this.firstName.textContent(),
  56  |       lastName: await this.lastName.textContent(),
  57  |       memberSince: await this.memberSince.textContent(),
  58  |     };
  59  |   }
  60  | 
  61  |   async enterDayAmount(amount: string) {
> 62  |     await expect(this.dayAmountInput).toBeVisible();
      |                                       ^ Error: expect(locator).toBeVisible() failed
  63  |     await expect(this.dayAmountInput).toBeEditable();
  64  |     await expect(this.dayAmountInput).not.toHaveValue("NaN");
  65  |     await expect(this.dayAmountInput).not.toHaveValue("");
  66  |     await this.dayAmountInput.fill(amount);
  67  |   }
  68  | 
  69  |   async saveChanges() {
  70  |     await this.saveBtn.click();
  71  |   }
  72  | 
  73  |   async confirmChanges() {
  74  |     await this.confirmBtn.waitFor({ timeout: 2000 }).catch(() => {});
  75  | 
  76  |     //adding condition to handle the situation when same amount is updated
  77  | 
  78  |     if (await this.confirmBtn.isVisible()) {
  79  |       await this.confirmBtn.click();
  80  |     }
  81  |   }
  82  | 
  83  |   async acknowledgeSuccess() {
  84  |     await this.continueBtn.click();
  85  |   }
  86  | 
  87  |   async verifyUpdatedValue(amount: string) {
  88  |     await expect(this.dayAmountInput).toHaveValue(amount);
  89  |   }
  90  | 
  91  |   async updateAndVerifyDailyEntryLimit(amount: string) {
  92  |     await this.enterDayAmount(amount);
  93  |     await this.saveChanges();
  94  |     await this.confirmChanges();
  95  |     await this.acknowledgeSuccess();
  96  |     await this.verifyUpdatedValue(amount);
  97  |   }
  98  | 
  99  |   /**
  100 |    * Assert that the share-to-feed checkbox is present in the settings UI
  101 |    * and currently in the expected state. Use for verifying the toggle is
  102 |    * rendered (the "from settings" verification the user asked for).
  103 |    */
  104 |   async assertFeedVisibleState(expected: boolean) {
  105 |     await expect(this.feedVisibleCheckbox).toBeVisible();
  106 |     await expect(this.feedVisibleCheckbox).toBeChecked({ checked: expected });
  107 |   }
  108 | 
  109 |   /**
  110 |    * Toggle `is_feed_visible` server-side using the same endpoint the My
  111 |    * Settings form posts to. The endpoint accepts a single-field payload
  112 |    * (see feed/tests/test_feed_entry.py::TestSettingsEndpointFeedVisibility),
  113 |    * which sidesteps react-hook-form's cross-field validators — those fail
  114 |    * the whole save when an unrelated limit field is in a bad state, which
  115 |    * is true on shared dev DBs that other tests have touched.
  116 |    */
  117 |   async setFeedVisibleViaAPI(enabled: boolean) {
  118 |     // Fire from inside the page context so the request inherits the
  119 |     // browser cookie jar (session + CSRF) and matches what the running
  120 |     // frontend would send. A raw page.request.post is rejected with 403
  121 |     // because Django's CSRF middleware needs the X-CSRFToken header to
  122 |     // match the csrftoken cookie.
  123 |     const result = await this.page.evaluate(async (isEnabled: boolean) => {
  124 |       const readCookie = (name: string): string => {
  125 |         const match = document.cookie
  126 |           .split(";")
  127 |           .map((c) => c.trim())
  128 |           .find((c) => c.startsWith(`${name}=`));
  129 |         return match ? decodeURIComponent(match.split("=").slice(1).join("=")) : "";
  130 |       };
  131 |       const post = await fetch("/api/v1/account/information/update/", {
  132 |         method: "POST",
  133 |         credentials: "include",
  134 |         headers: {
  135 |           "Content-Type": "application/json",
  136 |           "X-Requested-With": "XMLHttpRequest",
  137 |           "X-Parlay-Request": "1",
  138 |           "X-CSRFToken": readCookie("csrftoken"),
  139 |         },
  140 |         body: JSON.stringify({ is_feed_visible: isEnabled }),
  141 |       });
  142 |       const postBody = await post.text().catch(() => "");
  143 |       const get = await fetch("/api/v1/user/", {
  144 |         method: "GET",
  145 |         credentials: "include",
  146 |         headers: {
  147 |           "X-Requested-With": "XMLHttpRequest",
  148 |           "X-Parlay-Request": "1",
  149 |         },
  150 |       });
  151 |       const user = await get.json().catch(() => null);
  152 |       return {
  153 |         postStatus: post.status,
  154 |         postBody,
  155 |         getStatus: get.status,
  156 |         isFeedVisible: user?.isFeedVisible ?? null,
  157 |       };
  158 |     }, enabled);
  159 | 
  160 |     expect(
  161 |       result.postStatus,
  162 |       `setFeedVisible(${enabled}) POST failed: ${result.postBody}`,
```