# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: modals/userlimits.spec.ts >> User limit modal >> Verify user limit modal
- Location: tests/modals/userlimits.spec.ts:18:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('dialog', { name: /User Limit Exceeded/i }).filter({ visible: true }).first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('dialog', { name: /User Limit Exceeded/i }).filter({ visible: true }).first()

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e2]:
    - main [ref=e4]:
      - generic [ref=e8]:
        - generic [ref=e10]: Picks Submitted
        - generic [ref=e11]:
          - generic [ref=e12]:
            - generic [ref=e13]:
              - generic [ref=e14]: All-in Play
              - generic [ref=e15]: 07/17/2026
            - generic [ref=e16]:
              - generic [ref=e17]:
                - generic [ref=e18]: Multiplier
                - generic [ref=e19]: 6.49x
              - generic [ref=e20]:
                - generic [ref=e21]: $6.00 To Win
                - generic [ref=e22]: $38.94
            - generic [ref=e23]:
              - generic [ref=e24]:
                - generic [ref=e25]: "1."
                - generic [ref=e27]: Bukayo Saka
                - generic [ref=e28]:
                  - generic [ref=e29]: More
                  - img [ref=e30]
                  - generic [ref=e32]: "1.50"
                  - generic [ref=e33]: Shots
              - generic [ref=e34]:
                - generic [ref=e35]: "2."
                - generic [ref=e37]: Elliot Anderson
                - generic [ref=e38]:
                  - generic [ref=e39]: More
                  - img [ref=e40]
                  - generic [ref=e42]: "0.50"
                  - generic [ref=e43]: Shots
              - generic [ref=e44]:
                - generic [ref=e45]: "3."
                - generic [ref=e47]: Harry Kane
                - generic [ref=e48]:
                  - generic [ref=e49]: More
                  - img [ref=e50]
                  - generic [ref=e52]: "2.50"
                  - generic [ref=e53]: Shots
              - generic [ref=e54]:
                - generic [ref=e55]: "4."
                - generic [ref=e57]: Anthony Gordon
                - generic [ref=e58]:
                  - generic [ref=e59]: More
                  - img [ref=e60]
                  - generic [ref=e62]: "1.50"
                  - generic [ref=e63]: Shots
              - generic [ref=e64]:
                - generic [ref=e65]: "5."
                - generic [ref=e67]: Julián Alvarez
                - generic [ref=e68]:
                  - generic [ref=e69]: More
                  - img [ref=e70]
                  - generic [ref=e72]: "1.50"
                  - generic [ref=e73]: Shots
          - generic [ref=e74]: Track your lineup on the Leaderboard against other participants.
          - generic [ref=e75]:
            - button "share" [ref=e76] [cursor=pointer]:
              - img "share" [ref=e77]
            - button "Add Description" [ref=e78] [cursor=pointer]
            - button "Continue" [ref=e79] [cursor=pointer]
    - region "Notifications Alt+T"
  - alert [ref=e80]: ParlayPlay | Fun Fantasy Sports - Contest Submitted
  - iframe [ref=e81]:
    
```

# Test source

```ts
  1   | import { BasePage } from "./base.page";
  2   | import { expect, type Page } from "@playwright/test";
  3   | import { parseStatValue } from "@utils/helpers";
  4   | 
  5   | export class ContestPage extends BasePage {
  6   |     readonly heading = this.byText('Make Your More/Less Picks');
  7   |     readonly placePickBtn = this.locator('#place-pick');
  8   |     readonly entryAmountField = this.locator('#amount');
  9   |     readonly limitModal = this.byRole('dialog', { name: /User Limit Exceeded/i });
  10  |     readonly resetLimitButton = this.byRole('button', { name: 'Reset Limits' });
  11  |     readonly promoAppliedButton = this.locator('button.bg-playYellow');
  12  |     readonly payoutValues = this.locator('p.font-museo.text-xl')
  13  |         .filter({ visible: true });
  14  |     readonly errorToast = this.page
  15  |         .locator('.Toastify__toast--error .Toastify__toast-body')
  16  |         .filter({ visible: true })
  17  |         .first();
  18  |     // Post-submit success signal: the confirmation screen's heading
  19  |     // (contestConfirmation.picksSubmitted). Always present (unlike the share
  20  |     // button, which hides for live picks), so it's a reliable best-effort
  21  |     // settle after a 200 OK.
  22  |     readonly successIndicator = this.page
  23  |         .getByText('Picks Submitted', { exact: true })
  24  |         .filter({ visible: true })
  25  |         .first();
  26  |     // The PromoValidationBanner header on a promo conflict / unmet-qualifier
  27  |     // slip. When it shows, promoValidation.status is "ineligible", which
  28  |     // disables #place-pick (see amount.tsx) — so submitting would hang on the
  29  |     // never-actionable button. We surface it as a retryable failure instead.
  30  |     readonly promoConflictBanner = this.page
  31  |         .getByText(/Multiple Promos Applied|Missing Qualifiers/i)
  32  |         .filter({ visible: true })
  33  |         .first();
  34  | 
  35  |     constructor(page: Page) {
  36  |         super(page);
  37  |     }
  38  | 
  39  |     async verifyPage(): Promise<void> {
  40  |         await expect(this.heading).toBeVisible();
  41  |     };
  42  | 
  43  |     async placePick(): Promise<void> {
  44  |         await expect(this.placePickBtn).toBeVisible();
  45  |         // Toastify toasts (e.g. "N pick(s) loaded from shared slip" on the
  46  |         // tail flow) float over the submit area on the mobile viewport and
  47  |         // intercept pointer events until they auto-dismiss — long enough to
  48  |         // exhaust the bounded click below. They're close-on-click, so dismiss
  49  |         // any that are showing instead of racing their timeout.
  50  |         const toasts = this.page.locator('.Toastify__toast--close-on-click');
  51  |         for (let i = await toasts.count(); i > 0; i--) {
  52  |             await toasts.first().click({ timeout: 2_000 }).catch(() => {});
  53  |         }
  54  |         // Bounded click: if the button is disabled (e.g. a promo conflict sets
  55  |         // promoValidation.status="ineligible"), auto-waiting on actionability
  56  |         // would otherwise hang until the whole test times out. Fail fast with a
  57  |         // clear error instead. Callers that can recover use submitAndAwaitResult,
  58  |         // which detects the blocked state and retries with new cards.
  59  |         await this.placePickBtn.click({ timeout: 30_000 });
  60  |     };
  61  | 
  62  |     async setEntryAmountIfEditable(amount: string | number): Promise<void> {
  63  |         const readOnlyAttr = await this.entryAmountField.getAttribute('readonly');
  64  |         const isReadOnly = readOnlyAttr !== null;
  65  |         if(!isReadOnly) {
  66  |             const value = amount.toString();
  67  |             await this.entryAmountField.fill(value);
  68  |             await expect(this.entryAmountField).toHaveValue(value);
  69  |         };
  70  |     };
  71  | 
  72  |     async verifyLimitModalVisible(): Promise<void> {
> 73  |         await expect(this.limitModal).toBeVisible();
      |                                       ^ Error: expect(locator).toBeVisible() failed
  74  |     };
  75  | 
  76  |     async clickResetLimit(): Promise<void> {
  77  |         await this.resetLimitButton.click();
  78  |     };
  79  | 
  80  |     async verifyPromoIsSelected(): Promise<void> {
  81  |         await expect(this.promoAppliedButton).toBeVisible();
  82  |     };
  83  | 
  84  |     async getMaxPayout(): Promise<number> {
  85  |         const firstPayout = this.payoutValues.first();
  86  |         await expect(firstPayout).toBeVisible();
  87  |         const text = await firstPayout.textContent();
  88  |         return parseStatValue(text);
  89  |     };
  90  | 
  91  |     async submitAndAwaitResult(timeoutMs = 15000): Promise<{ success: boolean; kind: 'success' | 'response' | 'timeout' | 'blocked'; error: string | null }> {
  92  |         // A promo conflict ("Multiple Promos Applied") — or any unmet-qualifier
  93  |         // slip — sets promoValidation.status="ineligible", which disables
  94  |         // #place-pick. clicking a disabled button auto-waits on actionability
  95  |         // and hangs until the whole test times out. Detect the blocked state
  96  |         // and return a retryable failure so the caller re-picks with new cards.
  97  |         if (await this.promoConflictBanner.isVisible().catch(() => false)) {
  98  |             return {
  99  |                 success: false,
  100 |                 kind: 'blocked',
  101 |                 error: 'multiple promos applied (place-pick disabled)',
  102 |             };
  103 |         }
  104 |         // Give transient disables (payout/metadata loading) a moment to clear;
  105 |         // if the button is still not actionable, bail rather than hang.
  106 |         try {
  107 |             await expect(this.placePickBtn).toBeEnabled({ timeout: 8_000 });
  108 |         } catch {
  109 |             const conflict = await this.promoConflictBanner
  110 |                 .isVisible()
  111 |                 .catch(() => false);
  112 |             return {
  113 |                 success: false,
  114 |                 kind: 'blocked',
  115 |                 error: conflict
  116 |                     ? 'multiple promos applied (place-pick disabled)'
  117 |                     : 'place-pick stayed disabled (submission blocked)',
  118 |             };
  119 |         }
  120 | 
  121 |         // Use the actual /challenges-sp/crossgame/ POST status as the source of
  122 |         // truth. UI signals (success indicator vs error toast) raced badly:
  123 |         // a delayed/unrelated toast or a slow success-page navigation could
  124 |         // make a 200-OK contest look like a failure, triggering placeContestWithRetry
  125 |         // to submit a SECOND entry. The response status is unambiguous.
  126 |         const responsePromise = this.page
  127 |             .waitForResponse(
  128 |                 (resp) =>
  129 |                     /\/api\/v1\/challenges-sp\/crossgame\/?$/.test(new URL(resp.url()).pathname) &&
  130 |                     resp.request().method() === 'POST',
  131 |                 { timeout: timeoutMs },
  132 |             )
  133 |             .catch(() => null);
  134 | 
  135 |         await this.placePick();
  136 |         const resp = await responsePromise;
  137 | 
  138 |         if (!resp) {
  139 |             return {
  140 |                 success: false,
  141 |                 kind: 'timeout',
  142 |                 error: `No /challenges-sp/crossgame/ response within ${timeoutMs}ms`,
  143 |             };
  144 |         }
  145 | 
  146 |         if (resp.ok()) {
  147 |             // Best-effort UI sync so callers can keep clicking through; not a
  148 |             // success gate.
  149 |             await this.successIndicator
  150 |                 .waitFor({ state: 'visible', timeout: 5_000 })
  151 |                 .catch(() => undefined);
  152 |             return { success: true, kind: 'success', error: null };
  153 |         }
  154 | 
  155 |         // Server rejected — pull the message for retry-decision visibility.
  156 |         let errText = `HTTP ${resp.status()}`;
  157 |         try {
  158 |             const body = await resp.json();
  159 |             errText = body?.detail ?? body?.error ?? JSON.stringify(body).slice(0, 200);
  160 |         } catch {
  161 |             try {
  162 |                 errText = (await resp.text()).slice(0, 200) || errText;
  163 |             } catch {
  164 |                 /* keep status-only */
  165 |             }
  166 |         }
  167 |         return { success: false, kind: 'response', error: `${resp.status()} ${errText}` };
  168 |     };
  169 | };
  170 | 
```