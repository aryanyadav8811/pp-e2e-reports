# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: slip-persistence/slip-persistence.spec.ts >> Slip persistence - navigation and reload >> Slip persists through a Rewards tab round-trip
- Location: tests/slip-persistence/slip-persistence.spec.ts:56:5

# Error details

```
TimeoutError: locator.waitFor: Timeout 30000ms exceeded.
Call log:
  - waiting for locator('[data-testid="cross-game-card-wrapper"]').filter({ visible: true }).first() to be visible

```

# Page snapshot

```yaml
- iframe [ref=e2]:
  
```

# Test source

```ts
  74  |   // pills wrapper has none, so fall back to the visible `league-*` pills.
  75  |   // Every consumer only asserts visibility.
  76  |   readonly leagueSelector = this.page
  77  |     .getByTestId('league-selector')
  78  |     .or(this.page.locator('button[id^="league-"]'))
  79  |     .filter({ visible: true })
  80  |     .first();
  81  |   // Pill ids exist on both breakpoints; the container testid is mobile-only.
  82  |   readonly leagueButtons = this.page.locator('button[id^="league-"]').filter({ visible: true });
  83  |   readonly warningModal = this.visible(
  84  |     this.page.locator('div[role="dialog"]').filter({ hasText: 'Pick Warning' }),
  85  |   );
  86  |   readonly toggleMenu = this.byRole('button', { name: 'Toggle Menu' });
  87  |   // Button on mobile; link on desktop, with an optional badge-count suffix in
  88  |   // the accessible name.
  89  |   readonly rewardsTab = this.visible(
  90  |     this.page
  91  |       .getByRole('button', { name: 'Rewards' })
  92  |       .or(this.page.getByRole('link', { name: /^Rewards\b/ })),
  93  |   );
  94  |   // Anchored on pill ids for the same reason as leagueSelector — the
  95  |   // container-anchored form resolved the hidden mobile copy on Desktop.
  96  |   readonly fgleagueTabs = this.page
  97  |     .locator('button[id^="league-"]:not([id*="combo"]):not([id*="H1"]):not([id*="Q1"])')
  98  |     .filter({ visible: true });
  99  |   // Desktop has no Continue hop — the persistent pick-slip panel renders the
  100 |   // form inline and `#place-pick` carries mobile Continue's "slip is valid" semantics.
  101 |   readonly placePickBtn = this.locator('#place-pick');
  102 |   // "Slip is playable" on either breakpoint; specs assert on this rather than
  103 |   // `continueBtn` so they hold on both projects.
  104 |   readonly slipReadyCta = this.continueBtn.or(this.placePickBtn).first();
  105 |   readonly statsContainer = this.page.locator('div.flex.flex-col.items-center >> ul.flex.flex-row');
  106 | 
  107 |   readonly statsTabs = this.statsContainer.locator(':scope > li > button');
  108 | 
  109 |   constructor(page: Page) {
  110 |     super(page);
  111 |   }
  112 | 
  113 |   nthPlayerOnDefault(index: number): Locator {
  114 |     return this.playerCardsVisible.nth(index);
  115 |   }
  116 | 
  117 |   /**
  118 |    * The virtualised grid remounts cards as it scrolls, so `nth(i)` handles
  119 |    * silently re-resolve to a different card mid-flow; an id-anchored locator
  120 |    * always points at the same player.
  121 |    */
  122 |   playerCardById(playerId: string): Locator {
  123 |     return this.visible(this.page.locator(`div[id="${playerId}"]`));
  124 |   }
  125 | 
  126 |   /** Ids of the currently mounted player cards, in DOM order. */
  127 |   async listVisiblePlayerIds(): Promise<string[]> {
  128 |     await this.waitForPlayersGrid();
  129 |     await expect(this.playerCardsVisible.first()).toBeVisible();
  130 |     return (await this.playerCardsVisible.evaluateAll((els) => els.map((el) => el.id))).filter(
  131 |       Boolean,
  132 |     );
  133 |   }
  134 | 
  135 |   playerStatButton(id: string): Locator {
  136 |     return this.visible(this.page.locator(id).locator('button:has([data-testid="stat-value"])'));
  137 |   }
  138 | 
  139 |   // Pick buttons are `grid-button` in the mobile card but
  140 |   // `stat-selection-button` on desktop; a card only resolves inside one
  141 |   // tree, so the union is safe.
  142 |   pickButtons(card: Locator): Locator {
  143 |     return card.getByTestId('grid-button').or(card.getByTestId('stat-selection-button'));
  144 |   }
  145 | 
  146 |   moreLessDefaultPlayerButtons(index: number): Locator {
  147 |     return this.pickButtons(this.nthPlayerOnDefault(index));
  148 |   }
  149 | 
  150 |   async openTermsPageUsingNonAuth(button: 'Sign Up' | 'Join Now'): Promise<void> {
  151 |     if (button == 'Sign Up') {
  152 |       await this.signupButton.click();
  153 |     } else if (button == 'Join Now') {
  154 |       await this.joinNowButton.click();
  155 |     }
  156 |   }
  157 | 
  158 |   async waitForPlayersGrid(): Promise<void> {
  159 |     // The wrapper's inner shape differs per breakpoint — anchor on the outcome
  160 |     // (a mounted card, or the empty-state label) instead of its structure.
  161 |     await expect(
  162 |       this.playerCardsVisible.first().or(this.noPlayerFoundVisible).first(),
  163 |     ).toBeVisible();
  164 |   }
  165 | 
  166 |   async waitForFeedReady(timeout = 30_000): Promise<void> {
  167 |     // The losing waitFor keeps running after the race settles and rejects once
  168 |     // the page closes — Allure recorded that as a failed hook on passing tests.
  169 |     const swallow = (p: Promise<void>) => {
  170 |       p.catch(() => {});
  171 |       return p;
  172 |     };
  173 |     await Promise.race([
> 174 |       swallow(this.crossGameCardWrapper.waitFor({ state: 'visible', timeout })),
      |                                         ^ TimeoutError: locator.waitFor: Timeout 30000ms exceeded.
  175 |       swallow(this.noPlayerFoundVisible.waitFor({ state: 'visible', timeout })),
  176 |     ]);
  177 |   }
  178 | 
  179 |   async isEmptyState(): Promise<boolean> {
  180 |     return this.noPlayerFoundVisible.isVisible().catch(() => false);
  181 |   }
  182 | 
  183 |   async getPlayerCardCount(): Promise<number> {
  184 |     await this.waitForPlayersGrid();
  185 |     const cards = this.playerCardsVisible;
  186 |     await expect(cards.first()).toBeVisible();
  187 |     return await cards.count();
  188 |   }
  189 | 
  190 |   async listPlayersCard(): Promise<Locator[]> {
  191 |     await this.waitForPlayersGrid();
  192 |     const cards = this.playerCardsVisible;
  193 |     await expect(cards.first()).toBeVisible();
  194 |     const count = await cards.count();
  195 |     const result: Locator[] = [];
  196 |     for (let i = 0; i < count; i++) {
  197 |       result.push(cards.nth(i));
  198 |     }
  199 |     return result;
  200 |   }
  201 | 
  202 |   async getLeagueCount(): Promise<number> {
  203 |     return await this.leagueButtons.count();
  204 |   }
  205 | 
  206 |   /**
  207 |    * WebKit occasionally leaves a card image overlapping the league pill row
  208 |    * after submit → home, and an unbounded click retries against it until the
  209 |    * test times out. Scrolling to the top restores the row's hit area.
  210 |    */
  211 |   async clickLeagueTab(btn: Locator): Promise<void> {
  212 |     try {
  213 |       await btn.click({ timeout: 10_000 });
  214 |       return;
  215 |     } catch {
  216 |       /* intercepted — try to restore the layout */
  217 |     }
  218 |     await this.page.evaluate(() => window.scrollTo(0, 0));
  219 |     try {
  220 |       await btn.click({ timeout: 10_000 });
  221 |       return;
  222 |     } catch {
  223 |       /* still intercepted — last resort below */
  224 |     }
  225 |     await btn.click({ force: true, timeout: 10_000 });
  226 |   }
  227 | 
  228 |   async listLeagueButtons(): Promise<Locator[]> {
  229 |     const n = await this.leagueButtons.count();
  230 |     const leagueButtonsLocator: Locator[] = [];
  231 |     for (let i = 0; i < n; i++) {
  232 |       leagueButtonsLocator.push(this.leagueButtons.nth(i));
  233 |     }
  234 |     return leagueButtonsLocator;
  235 |   }
  236 | 
  237 |   /**
  238 |    * "Is the slip valid?" oracle for pickPlayers. Mobile: the Continue button.
  239 |    * Desktop has no Continue — the persistent panel mounts `#place-pick` once
  240 |    * the slip is playable and shows the "Let's Start" box otherwise.
  241 |    */
  242 |   async isContinueEnabled(): Promise<boolean> {
  243 |     if (await this.continueBtn.isVisible().catch(() => false)) {
  244 |       return this.continueBtn.isEnabled().catch(() => false);
  245 |     }
  246 |     if (await this.placePickBtn.isVisible().catch(() => false)) {
  247 |       // Payout/metadata loading briefly disables place-pick right after a
  248 |       // pick lands — give that a moment before reading the slip as invalid.
  249 |       return expect(this.placePickBtn)
  250 |         .toBeEnabled({ timeout: 3_000 })
  251 |         .then(
  252 |           () => true,
  253 |           () => false,
  254 |         );
  255 |     }
  256 |     return false;
  257 |   }
  258 | 
  259 |   async trySelectPick(card: Locator): Promise<boolean> {
  260 |     const buttons = this.pickButtons(card);
  261 |     const lessButton = buttons.first();
  262 |     // Filter updates run in useTransition and the grid is virtualised, so a
  263 |     // card from listVisiblePlayerIds can unmount before we act on it. With no
  264 |     // actionTimeout configured that would hang — treat "gone" as unpickable.
  265 |     let isEnabled: boolean;
  266 |     try {
  267 |       isEnabled = await lessButton.isEnabled({ timeout: 5000 });
  268 |     } catch {
  269 |       return false;
  270 |     }
  271 | 
  272 |     try {
  273 |       await buttons.nth(isEnabled ? 0 : 1).click({ timeout: 10_000 });
  274 |     } catch {
```