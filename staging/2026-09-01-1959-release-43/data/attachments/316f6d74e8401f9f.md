# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: signup/navigation.spec.ts >> Signup - Terms navigation >> Deeplink with referrals pre-populates
- Location: tests/signup/navigation.spec.ts:31:5

# Error details

```
Error: page.goto: Test ended.
Call log:
  - navigating to "https://staging.parlayplay.io/account/signup?coupon=oliviertest-705", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '../../fixtures/test.extend';
  2  | import { TermsPage } from '@pages/terms.page';
  3  | import { SignupPage } from '@pages/signup.page';
  4  | import { HomePage } from '@pages/home.page';
  5  | import { Users } from '../../data/users';
  6  | 
  7  | test.describe('Signup - Terms navigation', { tag: ['@signup', '@terms'] }, () => {
  8  |     test('terms page --> signup page', async ({ page }) => {
  9  |         const termsPage = new TermsPage(page);
  10 |         await termsPage.openTermsPage();
  11 |         await termsPage.assertReady();
  12 |         await expect(termsPage.createAccountbutton).toBeDisabled();
  13 |         await termsPage.setAccept(true);
  14 |         await expect(termsPage.createAccountbutton).toBeEnabled();
  15 |         await termsPage.proceed();
  16 |         await new SignupPage(page).assertReady();
  17 |     });
  18 | 
  19 |     test('Open Terms page from header Join Now', async({ page }) => {
  20 |         const homePage = new HomePage(page);
  21 |         await homePage.openTermsPageUsingNonAuth('Join Now');
  22 |         await new TermsPage(page).assertReady();
  23 |     })
  24 | 
  25 |     test('Open signup using footer Sign Up', async ({ page }) => {
  26 |         const homePage = new HomePage(page);
  27 |         await homePage.openTermsPageUsingNonAuth('Sign Up');
  28 |         await new TermsPage(page).assertReady();
  29 |     })
  30 | 
  31 |     test('Deeplink with referrals pre-populates', async ({ page }) => {
> 32 |         page.goto(`/account/signup?coupon=${Users.registered.referralCode}`);
     |              ^ Error: page.goto: Test ended.
  33 |         const termsPage = new TermsPage(page);
  34 |         await termsPage.assertReady();
  35 |         await termsPage.setAccept(true);
  36 |         await expect(termsPage.createAccountbutton).toBeEnabled();
  37 |         await termsPage.proceed();
  38 |         const signupPage = new SignupPage(page);
  39 |         await signupPage.assertReady();
  40 |         await expect(signupPage.referral).toHaveValue(`${Users.registered.referralCode}`)
  41 |     })
  42 | 
  43 | })
```