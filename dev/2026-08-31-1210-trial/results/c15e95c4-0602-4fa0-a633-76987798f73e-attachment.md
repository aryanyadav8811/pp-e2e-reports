# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: navigation/entries-nav-indicator.spec.ts >> Bottom nav — Entries active indicator (DFS-1816) >> Indicator stays under "Entries" when toggling Active and History
- Location: tests/navigation/entries-nav-indicator.spec.ts:27:5

# Error details

```
Error: page.waitForURL: Target page, context or browser has been closed
=========================== logs ===========================
waiting for navigation to "**/challenges/pending" until "load"
  navigated to "https://dev.parlayplay.io/account/login?showFreeSignup=true"
============================================================
```

# Test source

```ts
  74  |     /**
  75  |      * Waits until the entries list has settled — either the first card is
  76  |      * visible or the "Browse now" empty-state CTA rendered.
  77  |      */
  78  |     async waitForEntriesReady(timeout = 15_000): Promise<void> {
  79  |         await Promise.race([
  80  |             this.cardDivs.first().waitFor({ state: 'visible', timeout }),
  81  |             this.browseNow.waitFor({ state: 'visible', timeout }),
  82  |         ]);
  83  |     }
  84  | 
  85  |     async enterHistorytab() {
  86  |         await this.historyTab.click();
  87  |     }
  88  | 
  89  |     async selectTimeRangeValue(range: '7 Days' | '30 Days' | '6 Months'): Promise<void> {
  90  |         await this.byRole('button', { name: range }).click();
  91  |     }
  92  | 
  93  |     readonly prevArrow = this.locator('button:has(svg.fa-arrow-left)');
  94  | 
  95  |     async getEntriesCount() {
  96  |         let total = await this.cardDivs.count();
  97  | 
  98  |         // If no pagination or next arrow is already disabled, return current count
  99  |         if (!await this.nextArrow.isVisible().catch(() => false)) return total;
  100 | 
  101 |         // Navigate forward through all remaining pages
  102 |         do {
  103 |             await this.nextArrow.click();
  104 |             await this.cardDivs.first().waitFor({ state: 'visible' });
  105 |             total += await this.cardDivs.count();
  106 |         } while (!await this.nextArrow.isDisabled());
  107 | 
  108 |         // Navigate back to first page using left arrow
  109 |         while (!await this.prevArrow.isDisabled()) {
  110 |             await this.prevArrow.click();
  111 |             await this.cardDivs.first().waitFor({ state: 'visible' });
  112 |         }
  113 | 
  114 |         return total;
  115 |     }
  116 | 
  117 |     async verifyNumberGreaterThan(minCount: number, timeout = 15_000) {
  118 |         // Let the initial history fetch settle before traversing pagination.
  119 |         await this.waitForEntriesReady(timeout);
  120 |         const count = await this.getEntriesCount();
  121 |         expect(count).toBeGreaterThan(minCount);
  122 |     }
  123 | 
  124 |     async verifyCardsInFirstPage() {
  125 |         const cardsCount = await this.cardDivs.count();
  126 |         expect(cardsCount).toBe(this.cardsPerPage);
  127 |     }
  128 | 
  129 |     async getCardsPayoutValues() {
  130 |         const count = await this.cardDivs.count();
  131 | 
  132 |         for(let i = 0; i < count; i++) {
  133 |             const card = this.cardDivs.nth(i);
  134 |             //ToDo: Need to add new logic here, func is not in use currently
  135 |             const payoutValue = 0;
  136 |         }
  137 |     }
  138 | 
  139 |     async isNoDataVisible() {
  140 |         return await this.browseNow.isVisible();
  141 |     }
  142 | 
  143 |     async getStats() {
  144 |         const rawEntryAmount = await this.getStatText(this.entryAmountStat);
  145 |         const rawMaxPayout = await this.getStatText(this.maxPayoutStat);
  146 | 
  147 |         return {
  148 |             entryAmount: parseStatValue(rawEntryAmount),
  149 |             maxPayout: parseStatValue(rawMaxPayout),
  150 |         };
  151 |     }
  152 | 
  153 |     async getHistoryStats() {
  154 |         const rawPlaced = await this.getStatText(this.entriesPlacedStat);
  155 |         const rawWon = await this.getStatText(this.entriesWonStat);
  156 |         const rawEntryAmount = await this.getStatText(this.historyEntryAmountStat);
  157 |         const rawTotalWon = await this.getStatText(this.totalWonStat);
  158 |         const rawBiggestPayout = await this.getStatText(this.biggestPayoutStat);
  159 | 
  160 |         return {
  161 |             placed: parseStatValue(rawPlaced),
  162 |             won: parseStatValue(rawWon),
  163 |             amount: parseStatValue(rawEntryAmount),
  164 |             totalWon: parseStatValue(rawTotalWon),
  165 |             biggestPayout: parseStatValue(rawBiggestPayout),
  166 |         };
  167 |     }
  168 | 
  169 |     async enterActiveTab() {
  170 |         await this.activeTab.click();
  171 |     }
  172 | 
  173 |     async assertActiveTabLoaded() {
> 174 |         await this.page.waitForURL('**/challenges/pending');
      |                         ^ Error: page.waitForURL: Target page, context or browser has been closed
  175 |     }
  176 | 
  177 |     async assertHistoryTabLoaded() {
  178 |         await this.page.waitForURL('**/challenges/history');
  179 |     }
  180 | 
  181 |     async getTotalPaginationPages() {
  182 |         return await this.getNumberedPaginationButtons.count();
  183 |     }
  184 | 
  185 |     async gotoTrueLastPage() {
  186 | 
  187 |         const count = await this.pageButtons.count();
  188 | 
  189 |         if (count === 0) return; // Case 1: <= 16 entries, already on only page
  190 | 
  191 |         // Case 2 and 3: some pagination exists
  192 |         const numbers: number[] = [];
  193 |         for (let i = 0; i < count; i++) {
  194 |             const txt = await this.pageButtons.nth(i).innerText();
  195 |             numbers.push(parseInt(txt, 10));
  196 |         }
  197 |         const maxVisible = Math.max(...numbers);
  198 | 
  199 |         // If maxVisible <= 5, last page is directly visible
  200 |         if (maxVisible <= 5) {
  201 |             await this.pageButtons.filter({ hasText: String(maxVisible) }).click();
  202 |         } else {
  203 |         // More than 5 pages:
  204 |         // 1) Go to page 5 (or the highest visible)
  205 |         await this.pageButtons.filter({ hasText: '5' }).click();
  206 |         const refreshed = this.pageButtons;
  207 |         const refreshedCount = await refreshed.count();
  208 |         const refreshedNumbers: number[] = [];
  209 |         for (let i = 0; i < refreshedCount; i++) {
  210 |             const txt = await refreshed.nth(i).innerText();
  211 |             refreshedNumbers.push(parseInt(txt, 10));
  212 |         }
  213 |         const lastPage = Math.max(...refreshedNumbers);
  214 |         await refreshed.filter({ hasText: String(lastPage) }).click();
  215 |         }
  216 | 
  217 |         await expect(this.nextArrow).toBeDisabled();
  218 |     }
  219 | 
  220 |     async verifyLastEntryIs(submissionId: string,) {
  221 |         await this.gotoTrueLastPage();
  222 | 
  223 | 
  224 |         await this.lastCard.scrollIntoViewIfNeeded();
  225 |         await this.lastCard.click();
  226 | 
  227 |         await expect(this.lastCard).toContainText(submissionId);
  228 |     }
  229 | 
  230 |     // ==================== ENTRY CARD — DFS-1696 REDESIGN ====================
  231 |     // The redesigned card (components/Entry/index.tsx) has two controls that
  232 |     // flip the same expand/collapse state: the whole header acts as the
  233 |     // primary toggle, and a dedicated chevron button sits at the bottom-right.
  234 |     // Both expose the same accessible name ("Expand entry details" when closed,
  235 |     // "Collapse entry details" when open), so we disambiguate purely by DOM
  236 |     // order — the header comes first, the chevron last.
  237 | 
  238 |     headerToggle(card: Locator): Locator {
  239 |         return card.getByRole('button', { name: /entry details/ }).first();
  240 |     }
  241 | 
  242 |     chevronToggle(card: Locator): Locator {
  243 |         return card.getByRole('button', { name: /entry details/ }).last();
  244 |     }
  245 | 
  246 |     /** Expands a card via its header (no-op if already open) and waits for the open state. */
  247 |     async expandCard(card: Locator): Promise<void> {
  248 |         if ((await card.getAttribute('aria-expanded')) === 'true') return;
  249 |         await this.headerToggle(card).click();
  250 |         await expect(card).toHaveAttribute('aria-expanded', 'true');
  251 |     }
  252 | 
  253 |     /**
  254 |      * Scans the cards on the current page, expanding each, and returns the first
  255 |      * one that exposes the "Cancel Entry" countdown button. An entry is only
  256 |      * cancellable within 5 minutes of creation (DFS-1983, tightened from 10) and
  257 |      * only while none of its picks have gone live. The matched card is left
  258 |      * expanded. Returns null when none on the current page are cancellable.
  259 |      */
  260 |     async findCancellableCardOnCurrentPage(): Promise<Locator | null> {
  261 |         const count = await this.cardDivs.count();
  262 |         for (let i = 0; i < count; i++) {
  263 |             const card = this.cardDivs.nth(i);
  264 |             await this.expandCard(card);
  265 |             const cancelButton = card.getByRole('button', { name: /Cancel Entry/ });
  266 |             if (await cancelButton.isVisible().catch(() => false)) return card;
  267 |         }
  268 |         return null;
  269 |     }
  270 | 
  271 |     /**
  272 |      * Finds the first cancellable entry across the whole Active list, not just
  273 |      * the first page. A freshly-placed entry sorts to the *end* of the list, so
  274 |      * it lands on the last page — with no pagination we scan the single page;
```